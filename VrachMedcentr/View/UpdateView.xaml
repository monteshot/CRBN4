<Window x:Class="VrachMedcentr.View.UpdateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VrachMedcentr"
        mc:Ignorable="d"
        Title="Оновлення..." Height="100" Width="350" WindowStartupLocation="CenterScreen">
    <Grid x:Name="GridUpdate">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding NameFile}"></TextBlock>
        <ProgressBar x:Name="ProgressBar" Value="{Binding progressBarValueProp}" Height="Auto"   Margin="5" Grid.Row="1">
            
        </ProgressBar>
        
    </Grid>
</Window>
